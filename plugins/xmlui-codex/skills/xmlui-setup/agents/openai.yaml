interface:
  display_name: "XMLUI Setup"
  short_description: "Install XMLUI CLI, configure MCP, and scaffold a project"
  default_prompt: "Use $xmlui-setup to install XMLUI, configure MCP, and scaffold a starter project."

policy:
  allow_implicit_invocation: true
